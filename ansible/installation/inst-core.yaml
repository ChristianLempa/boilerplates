- name: install core packages
  hosts: "{{ hosts }}"
  become: yes
  tasks:
  - name: install core packages
    apt:
      name:
        - htop
        - nfs-common
        - vim
        - tmux
        - ncdu
        - docker.io
        - docker-compose
        - samba
      update_cache: yes
