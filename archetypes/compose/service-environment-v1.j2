{# @requires services.*.environment #}
    environment:
      - TZ={{ container_timezone }}
      {# @optional-start #}
      {% if swarm_enabled %}
      - SECRET=/run/secrets/{{ service_name }}_secret_1
      {% else %}
      - SECRET=${SECRET}
      {% endif %}
      {# @optional-end #}
