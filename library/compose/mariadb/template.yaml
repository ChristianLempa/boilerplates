---
kind: compose
schema: "1.2"
metadata:
  name: Volumes
  description: Docker compose setup for volumes
  version: 12.0.2
  author: Christian Lempa
  date: '2025-09-28'
  tags: []
spec:
  general:
    vars:
      volumes_version:
        type: str
        description: Volumes version
        default: latest
