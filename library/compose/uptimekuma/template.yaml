---
kind: compose
metadata:
  name: Volumes
  description: Docker compose setup for volumes
  version: 1.23.16
  author: Christian Lempa
  date: '2025-09-28'
  tags:
  - volumes
  - docker
  - compose
  draft: true
spec:
  general:
    vars:
      volumes_version:
        type: str
        description: Volumes version
        default: latest
