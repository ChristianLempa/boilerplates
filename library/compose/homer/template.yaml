---
kind: compose
metadata:
  name: Homer
  description: Docker compose setup for homer
  version: v25.08.1
  author: Christian Lempa
  date: '2025-09-28'
  tags:
    - dashboard
spec:
  ports:
    vars:
      ports_http:
        description: "Host port for HTTP (8080)"
        type: int
        default: 8080
