---
kind: compose
metadata:
  name: Volumes
  description: Docker compose setup for volumes
  version: 11.3
  author: Christian Lempa
  date: '2025-09-28'
  tags:
  - volumes
  - docker
  - compose
  draft: true
spec:
  general:
    vars:
      volumes_version:
        type: string
        description: Volumes version
        default: latest
