{
  "version": "1",
  "slug": "prometheus",
  "kind": "compose",
  "metadata": {
    "name": "Prometheus",
    "description": "Prometheus is an open-source systems monitoring and alerting toolkit originally built at SoundCloud.\nIt is designed for reliability and scalability, making it suitable for monitoring dynamic cloud environments.\nPrometheus collects and stores metrics as time series data, providing powerful querying capabilities and integration with various visualization tools.\n##  Swarm Deployment Warning\nPrometheus uses local TSDB storage and does NOT support running multiple replicas.\nThis template enforces a single replica with node placement constraints. For true HA, consider remote storage solutions (Thanos, Cortex, VictoriaMetrics).\n\nProject: https://prometheus.io/\n\nDocumentation: https://prometheus.io/docs/\n\nGitHub: https://github.com/prometheus/prometheus\n",
    "tags": [
      "traefik",
      "swarm",
      "authentik"
    ],
    "author": "Christian Lempa",
    "template_version": "v3.9.1",
    "icon": {
      "provider": "selfh",
      "id": "prometheus"
    },
    "date": "2026-01-07",
    "next_steps": "{% if swarm_enabled -%}\n1. Deploy to Docker Swarm:\n   docker stack deploy -c compose.yaml {{ service_name }}\n2. Access Prometheus:\n   {%- if traefik_enabled %} https://{{ traefik_host }}\n   {%- else %} http://<swarm-node-ip>:{{ ports_http }}{%- endif %}\n{% else -%}\n1. Start Prometheus with Docker Compose:\n   docker compose up -d\n2. Access Prometheus:\n   {%- if traefik_enabled %} https://{{ traefik_host }}\n   {%- else %} http://localhost:{{ ports_http }}{%- endif %}\n{% endif -%}\n3. Edit config/prometheus.yaml to add scrape targets\n4. Reload configuration: docker exec {{ container_name if not swarm_enabled else service_name }} kill -HUP 1\n"
  },
  "entrypoint": {
    "path": "compose.yaml",
    "type": "file"
  },
  "variables": [
    {
      "key": "service_name",
      "type": "string",
      "label": "Service Name",
      "required": false,
      "section": {
        "key": "general"
      },
      "source_type": "str",
      "default": "prometheus"
    },
    {
      "key": "restart_policy",
      "type": "string",
      "label": "Restart Policy",
      "required": true,
      "section": {
        "key": "general"
      },
      "source_type": "enum",
      "default": "unless-stopped",
      "options": [
        "unless-stopped",
        "always",
        "on-failure",
        "no"
      ]
    },
    {
      "key": "metrics_retention_time",
      "type": "string",
      "label": "How long to retain samples (e.g., 15d, 30d, 1y)",
      "required": false,
      "section": {
        "key": "metrics",
        "title": "Metrics & Storage",
        "description": "Configure data retention and storage settings"
      },
      "source_type": "str",
      "default": "15d",
      "description": "How long to retain samples (e.g., 15d, 30d, 1y)",
      "extra": "Older data will be deleted. Use 'h', 'd', 'w', 'y' for time units."
    },
    {
      "key": "metrics_retention_size",
      "type": "string",
      "label": "Maximum storage size (e.g., 5GB, 10GB, 1TB)",
      "required": false,
      "section": {
        "key": "metrics",
        "title": "Metrics & Storage",
        "description": "Configure data retention and storage settings"
      },
      "source_type": "str",
      "default": "0",
      "description": "Maximum storage size (e.g., 5GB, 10GB, 1TB)",
      "extra": "Set to 0 for unlimited. Triggers deletion when exceeded."
    },
    {
      "key": "metrics_enable_remote_write",
      "type": "boolean",
      "label": "Enable remote write receiver (allows pushing metrics via /api/v1/write)",
      "required": false,
      "section": {
        "key": "metrics",
        "title": "Metrics & Storage",
        "description": "Configure data retention and storage settings"
      },
      "source_type": "bool",
      "default": false,
      "description": "Enable remote write receiver (allows pushing metrics via /api/v1/write)",
      "extra": "Caution: Intended for low-volume use cases only. Not efficient for general ingestion."
    },
    {
      "key": "ports_http",
      "type": "string",
      "label": "Ports Http",
      "required": false,
      "section": {
        "key": "ports"
      },
      "source_type": "str",
      "default": 9090
    },
    {
      "key": "traefik_host",
      "type": "string",
      "label": "Traefik Host",
      "required": false,
      "section": {
        "key": "traefik",
        "title": "Traefik",
        "toggle": "traefik_enabled",
        "description": "Configure Traefik reverse proxy integration"
      },
      "source_type": "str",
      "default": "prometheus"
    },
    {
      "key": "traefik_network",
      "type": "string",
      "label": "Traefik Network",
      "required": true,
      "section": {
        "key": "traefik",
        "title": "Traefik",
        "toggle": "traefik_enabled",
        "description": "Configure Traefik reverse proxy integration"
      },
      "source_type": "str",
      "default": "traefik"
    },
    {
      "key": "traefik_domain",
      "type": "string",
      "label": "Traefik Domain",
      "required": true,
      "section": {
        "key": "traefik",
        "title": "Traefik",
        "toggle": "traefik_enabled",
        "description": "Configure Traefik reverse proxy integration"
      },
      "source_type": "str",
      "default": "home.arpa"
    },
    {
      "key": "traefik_enabled",
      "type": "boolean",
      "label": "Enable Traefik integration",
      "required": false,
      "section": {
        "key": "traefik",
        "title": "Traefik",
        "toggle": "traefik_enabled",
        "description": "Configure Traefik reverse proxy integration"
      },
      "source_type": "bool",
      "default": false,
      "description": "Enable Traefik integration"
    },
    {
      "key": "traefik_tls_certresolver",
      "type": "string",
      "label": "Traefik Tls Certresolver",
      "required": true,
      "section": {
        "key": "traefik_tls",
        "title": "Traefik TLS",
        "toggle": "traefik_tls_enabled",
        "needs": "traefik",
        "description": "Configure Traefik TLS/SSL certificates"
      },
      "source_type": "str",
      "default": "cloudflare"
    },
    {
      "key": "traefik_tls_enabled",
      "type": "boolean",
      "label": "Enable Traefik TLS",
      "required": false,
      "section": {
        "key": "traefik_tls",
        "title": "Traefik TLS",
        "toggle": "traefik_tls_enabled",
        "needs": "traefik",
        "description": "Configure Traefik TLS/SSL certificates"
      },
      "source_type": "bool",
      "default": false,
      "description": "Enable Traefik TLS"
    }
  ],
  "legacy": {
    "schema": "1.2"
  }
}
