apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-data-claim
  namespace: nginx
spec:
  volumeName: nginx-data
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
